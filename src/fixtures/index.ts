/**
 * Deterministic, offline fixture portfolio for the family-office OS.
 *
 * Re-exports a realistic diverse portfolio spanning every asset class as typed
 * fixtures. Import these anywhere a downstream unit needs realistic sample data
 * without touching a live API. READ-ONLY product: fixtures describe state, they
 * never move money.
 */
export * from "./portfolio";

/**
 * Founder / investor relationship graph: a read-only projection unifying the
 * people, company-ownership, and deal-contact domains into a single graph for
 * display and analysis. Pure and deterministic — see {@link buildRelationshipGraph}.
 */
export * from "./relationship-graph";
export * from "./layout";

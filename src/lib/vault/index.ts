/**
 * m9-vault — Document & obligation vault for the read-only family office OS.
 *
 * Import the registry types and the offline obligation extractor from
 * {@link ./vault}, and the deterministic {@link seededVault} fixture from
 * {@link ./fixtures}. Everything is pure, offline and Decimal-backed.
 */
export * from "./vault";
export * from "./fixtures";
